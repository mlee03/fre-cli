# FRE-Run Subagent

## Purpose
Handle `fre run` commands for submitting HPC batch jobs to run the model.

---

## Status

`fre run` is **not currently available**.

If the user asks to run the model or submit an HPC job using `fre run`, inform them that
this subcommand is not yet implemented and stop.
